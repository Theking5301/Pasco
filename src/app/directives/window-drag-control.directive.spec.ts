import { WindowDragControlDirective } from './window-drag-control.directive';

describe('WindowDragControlDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowDragControlDirective();
    expect(directive).toBeTruthy();
  });
});
